{
  "url": "api/single/enrich/sync",
  "method": "POST",
  "body": {
    "entity_data": "{{parseJSON(parameters.entityData)}}",
    "schema_id": "{{parameters.schemaId}}",
    "models": "{{parameters.models}}",
    "languages": "{{ifempty(parameters.languages, array('en'))}}",
    "strategy": "{{ifempty(parameters.strategy, 'auto')}}",
    "classification_model": "{{parameters.classificationModel}}",
    "arbitration_model": "{{parameters.arbitrationModel}}",
    "enable_web_search": "{{ifempty(parameters.enableWebSearch, 'off') == 'on'}}",
    "enable_response_schema": "{{ifempty(parameters.enableResponseSchema, 'on') == 'on'}}",
    "enable_strict_structured_output": "{{ifempty(parameters.enableStrictStructuredOutput, 'off') == 'on'}}",
    "timeout_seconds": "{{ifempty(parameters.timeoutSeconds, 300)}}",
    "attachment_ids": "{{parameters.attachmentIds}}"
  }
}

