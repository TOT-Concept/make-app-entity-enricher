{
  "name": "generateSchema",
  "label": "Generate Schema",
  "description": "Generate and auto-save a JSON schema from 1..N sample objects of the same entity type (multi-sample: union of fields, nullable where a field is missing in some sample, real observed values as examples). The module blocks until generation completes or times out.",
  "connection": "apiKey",
  "altConnection": "oauth2",
  "typeId": 4,
  "actionType": "create"
}
