{
  "url": "{{connection.baseUrl}}/api/enrichment/options",
  "method": "GET",
  "response": {
    "output": "{{item}}",
    "iterate": {
      "container": "{{map(keys(body.languages), 'code', 'label', body.languages)}}"
    }
  }
}
