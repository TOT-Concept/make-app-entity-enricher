{
  "url": "{{connection.baseUrl}}/api/enrichment/options",
  "method": "GET",
  "response": {
    "output": "{{item}}",
    "iterate": {
      "container": "{{body.models}}",
      "condition": "{{item.is_available && (!item.processing_disabled || !item.processing_disabled.arbitration)}}"
    }
  }
}
