{
  "url": "api/schema/saved",
  "method": "GET",
  "response": {
    "iterate": "{{body.schemas}}",
    "output": {
      "name": "{{if(item.is_pinned, '📌 ' + item.name, item.name)}}",
      "id": "{{item.id}}"
    }
  }
}
