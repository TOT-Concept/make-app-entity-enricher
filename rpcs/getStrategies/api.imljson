{
  "url": "api/enrichment/options",
  "method": "GET",
  "response": {
    "output": {
      "label": "{{replace(capitalize(item.name), '_', ' ')}} — {{item.description}}",
      "value": "{{item.name}}"
    },
    "iterate": {
      "container": "{{body.strategies}}"
    }
  }
}
