{
  "name": "getStrictStructuredOutputOptions",
  "label": "Strict structured output availability",
  "description": "Returns ['On', 'Off'] only when at least one selected enrichment model declares supports_strict_structured_output; otherwise returns just 'Off'. Mirrors the n8n connector behavior.",
  "connection": "apiKey"
}
