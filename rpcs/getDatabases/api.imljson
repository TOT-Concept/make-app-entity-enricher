{
  "url": "api/schemas/{{parameters.schemaId}}/databases",
  "method": "GET",
  "response": {
    "iterate": "{{body.databases}}",
    "output": {
      "name": "{{item.name}} ({{item.dialect}})",
      "id": "{{item.id}}"
    }
  }
}
